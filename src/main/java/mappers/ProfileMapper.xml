<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ProfileDAO">

	
	
	<!-- 등록 -->
	<!-- <insert id="insertProfile" parameterType="profile">
		INSERT INTO host(host_id, host_pic, company, uni, host_value)
		VALUES(member_id, #{name}, #{email}, #{password}, ${tel}, 0, SYSDATE, SYSDATE)
	</insert> -->
 
 	<!-- 단순 조회 -->
	<!-- <select id="getMember" resultType="profile">
		SELECT *
		FROM member
		WHERE email = #{email}
	</select>
 	
 	<select id="getMemberList" resultType="profile">
		SELECT *
		FROM member
	</select> -->
	
</mapper>